#ifndef OPTIMIZATION_H
#define OPTIMIXATION_H
#include <regex>
#include <database/db.h>

class optimization {
    // Class that represents optimizing code
    public:
        /**
         * (optimization): default constructor that will optimize your code
         * source: is a string that holds the whole file
         */
        optimization(const *char source);

};

#endif
